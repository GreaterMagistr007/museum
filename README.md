# Музей «Иркутское юнкерское училище»

Информационный сайт музея «Иркутское юнкерское училище» (внештатное музейное образование, г. Иркутск).

Здание бывшего юнкерского училища расположено по адресу: ул. 5-й Армии, 65 / ул. Ярослава Гашека, 5.

Три исторических периода:
- Иркутское юнкерское (военное) училище (1874–1918)
- Пехотные курсы командиров РККА (1920–1933)
- Топографический отряд (1934 г. – н.в.)

## Стек

- **PHP** 8.2, **Laravel** 11
- **Blade** — шаблонизатор
- **SQLite** (dev) / **MySQL** (prod, БД `museum_db`)
- **CSS** — единый файл `server/public/css/style.css` (BEM, CSS Custom Properties, 3 breakpoints: 1024px, 768px, 500px)
- **JS** — `server/public/js/main.js` (vanilla JS, без фреймворков)
- **Vite** — настроен, но не используется (ассеты отдаются из `public/` через `asset()`)

## Структура проекта

```
museum/
├── frontend/                     # HTML-прототип вёрстки (статические .html)
├── images/                       # Исходные изображения (не в Laravel)
├── server/                       # Laravel 11 приложение
│   ├── app/                      # Стандартные заглушки (Controller, User, AppServiceProvider)
│   ├── routes/web.php            # Все маршруты (23 GET-роута, замыкания)
│   ├── resources/views/
│   │   ├── layouts/app.blade.php # Основной layout
│   │   ├── components/           # header, footer, breadcrumbs, modals
│   │   └── pages/                # 15 blade-шаблонов страниц
│   ├── public/
│   │   ├── css/style.css         # Единый CSS (1362 строки)
│   │   ├── js/main.js            # Единый JS (183 строки)
│   │   └── images/               # Изображения для сайта
│   └── database/                 # SQLite, стандартные миграции Laravel
├── maket.png                     # Макет главной страницы
├── website-layout-explanations.txt # Описание макета
├── competitor-analysis.md        # Анализ сайтов конкурентов (см. ниже)
└── README.md
```

## Архитектура

Статический информационный сайт. Контроллеров нет — все роуты определены как замыкания в `routes/web.php`, каждый возвращает Blade-view. Модели предметной области не используются. Весь контент (новости, экспозиции, экскурсии, исторические статьи) захардкожен в Blade-шаблонах.

### Страницы

| Раздел | URL | Описание |
|---|---|---|
| Главная | `/` | Сайдбары с кнопками, дерево формирований, сетка экскурсий |
| Новости | `/news` | 10 новостных карточек |
| Экспозиция | `/exposition` | 6 тематических карточек |
| Архив | `/archive` | 6 категорий архивных материалов |
| О музее | `/about` | История, миссия, расписание |
| Контакты | `/contacts` | Адрес, телефон, email, Яндекс.Карты |
| Экскурсии | `/excursions` | 6 экскурсий с кратким описанием |
| Военный городок | `/military-town` | Обзорная статья |
| — Юнкерское училище | `/junker-school` | 1874–1918, подробная статья |
| — Пехотные курсы | `/infantry-courses` | 1920–1933, подробная статья |
| — Топографический отряд | `/topographic-unit` | 1934–н.в., подробная статья |
| Экскурсия: обзорная | `/excursion/overview` | По всему музею |
| Экскурсия: юнкерское | `/excursion/junker` | История юнкеров |
| Экскурсия: награды | `/excursion/awards` | Наградная система |
| Экскурсия: топослужба | `/excursion/topographic-service` | Топографическая служба |
| Экскурсия: Иркутский отряд | `/excursion/irkutsk-topographic` | Локальная история |
| Экскурсия: документы | `/excursion/documents` | Архивные документы и знаки |

### Интерактивные элементы

- 4 модальных окна на главной (О музее, Режим работы, Карта с Яндекс.Картами, Контакты)
- Бургер-меню (мобильные, ≤768px)
- Dropdown-подменю «Военный городок» (на мобильных — аккордеон)
- Аккордеон «Воинские формирования» на главной
- Хлебные крошки на всех внутренних страницах

### Внешние интеграции

- **Яндекс.Карты** — iframe-виджет на странице контактов и в модалке «Как нас найти»

## Запуск

```bash
cd server

# Установка зависимостей
composer install

# Настройка окружения
cp .env.example .env
php artisan key:generate

# Миграции
php artisan migrate

# Dev-сервер
php artisan serve
```

Сайт будет доступен по адресу `http://localhost:8000`.

## Линтинг и тесты

```bash
cd server

# Code style (Laravel Pint)
./vendor/bin/pint

# Тесты
php artisan test
```

## Анализ конкурентов

Файл [`competitor-analysis.md`](competitor-analysis.md) содержит подробный конкурентный анализ:

- **Топ музеев Иркутска** (7 сайтов): Тальцы, Художественный музей Сукачёва, Музей декабристов, Краеведческий музей и др.
- **Топ музеев РФ** (10 сайтов): Эрмитаж, Третьяковка, ГИМ, Музей Победы, военно-исторические музеи и др.
- **Сводная таблица** функционала (25+ параметров) с оценкой каждого конкурента
- **Рекомендации** — что добавить на сайт, разбитые по приоритетам (критический / высокий / средний / низкий) с обоснованием каждого пункта

### Ключевые выводы из анализа

Военно-исторические музеи РФ имеют слабые сайты. Основные пробелы нашего сайта относительно конкурентов:

**Критический приоритет:** реальные фотографии вместо плейсхолдеров, реальные контактные данные.

**Высокий приоритет:** онлайн-билеты, версия для слабовидящих, соцсети, форма обратной связи, SEO meta-теги, фотогалереи, виджет Госуслуг.

**Средний приоритет:** виртуальный тур, календарь мероприятий, онлайн-каталог экспонатов, образовательный раздел, аудиогид (izi.travel).

## Известные проблемы

- Контент захардкожен в Blade-шаблонах (нет CMS/админки, нет моделей)
- Плейсхолдеры изображений вместо реальных фото на страницах экспозиции, архива, новостей, экскурсий
- Контактные данные-заглушки: `+7 (3952) XX-XX-XX`, `museum@example.ru`
- Расхождение адресов: в модалке «ул. Советская, 176», на странице контактов «ул. Ярослава Гашека, 5»
- `APP_NAME=Laravel` и `APP_LOCALE=en` в `.env` не обновлены
- Vite настроен, но не используется
- `server.zip` (33 МБ) — бинарный артефакт в корне репозитория

## Deploy

Прод-сервер: `pkfsb`, пользователь `insite`, директория `/var/www/insite/data/www/museum.in-site.ru` (есть симлинк `~/www/museum.in-site.ru`). PHP — `/opt/php85/bin/php` (PHP 8.5). Composer и npm на сервере не установлены, поэтому `vendor/` уже залит на сервер вручную и обновляется руками при изменении зависимостей.

### Принцип

Содержимое серверной директории = содержимое `server/` из этого репозитория. На сервере держится bare-клон в `.museum-git/`, скрипт `deploy.sh` через `git archive master server/ | tar -x --strip-components=1` раскладывает свежие файлы поверх. Ветка деплоя — только `master`.

Удалённые из репозитория файлы скрипт сам **не сносит** — их нужно вычищать на сервере вручную после соответствующего релиза.

### Первый запуск (разовая настройка сервера)

1. Перенести `deploy.sh` на сервер в корень проекта:

   ```bash
   scp server/deploy.sh insite@pkfsb:/var/www/insite/data/www/museum.in-site.ru/deploy.sh
   ```

2. На сервере дописать в `.env` строку с GitHub Personal Access Token (scope: `repo` или `public_repo` — достаточно read):

   ```
   GIT_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

3. Запустить деплой — он сам склонирует bare-репозиторий и развернёт код:

   ```bash
   cd /var/www/insite/data/www/museum.in-site.ru
   bash deploy.sh
   ```

### Обычный релиз

```bash
ssh insite@pkfsb 'cd /var/www/insite/data/www/museum.in-site.ru && bash deploy.sh'
```

Скрипт сам обновится из репозитория (он лежит в `server/deploy.sh`), поэтому отдельно перезаливать его не нужно.

Что делает скрипт по шагам:
1. Включает maintenance (`artisan down --retry=5`).
2. Фетчит `master` в bare-клон `.museum-git/`.
3. Раскладывает содержимое `server/` поверх рабочей директории.
4. Чистит `bootstrap/cache/*` и пересобирает `package:discover` (защита от dev-провайдеров в `packages.php`).
5. Прогоняет `migrate --force`.
6. Сбрасывает и перепрогревает кэши: config / route / view.
7. Снимает maintenance (`artisan up`).

### Обновление vendor/

Если в `composer.json` поменялись зависимости — локально:

```bash
cd server
composer install --no-dev --optimize-autoloader
rsync -av --delete vendor/ insite@pkfsb:/var/www/insite/data/www/museum.in-site.ru/vendor/
```

Vite в проекте не используется, `public/build/` синхронизировать не нужно.

### Сидеры

По умолчанию `db:seed` не выполняется. Чтобы включить — раскомментировать соответствующую строку в `server/deploy.sh`. Включать только если сидеры идемпотентны и не плодят дубликаты при повторном запуске.
